{
  "name": "reitit-cf-demo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n cljs,worker -c blue,green \"npx shadow-cljs watch worker\" \"npx wrangler dev\"",
    "watch": "npx shadow-cljs watch worker",
    "release": "npx shadow-cljs release worker",
    "wrangler": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "shadow-cljs": "^2.28.0"
  },
  "dependencies": {
    "saxes": "^6.0.0",
    "wrangler": "^3.99.0"
  }
}
